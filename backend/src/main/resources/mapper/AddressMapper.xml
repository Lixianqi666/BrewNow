<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.brewnow.mapper.AddressMapper">

    <resultMap id="addressResultMap" type="com.brewnow.entity.Address">
        <id property="addressId" column="address_id"/>
        <result property="userId" column="user_id"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="detailAddress" column="detail_address"/>
        <result property="tag" column="tag"/>
        <result property="isDefault" column="is_default"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <sql id="addressColumns">
        address_id, user_id, receiver_name, contact_phone, province, city, district, detail_address, tag,
        is_default, create_time, update_time, deleted_at
    </sql>

    <select id="selectById" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM addresses
        WHERE address_id = #{addressId} AND deleted_at IS NULL
    </select>

    <select id="selectByUserId" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM addresses
        WHERE user_id = #{userId} AND deleted_at IS NULL
        ORDER BY is_default DESC, update_time DESC, address_id DESC
    </select>

    <select id="selectDefaultByUserId" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM addresses
        WHERE user_id = #{userId} AND is_default = 1 AND deleted_at IS NULL
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.brewnow.entity.Address" useGeneratedKeys="true" keyProperty="addressId">
        INSERT INTO addresses (
            user_id, receiver_name, contact_phone, province, city, district, detail_address, tag, is_default
        ) VALUES (
            #{userId}, #{receiverName}, #{contactPhone}, #{province}, #{city}, #{district}, #{detailAddress},
            #{tag}, #{isDefault}
        )
    </insert>

    <update id="updateById" parameterType="com.brewnow.entity.Address">
        UPDATE addresses
        <set>
            <if test="receiverName != null and receiverName != ''">
                receiver_name = #{receiverName},
            </if>
            <if test="contactPhone != null and contactPhone != ''">
                contact_phone = #{contactPhone},
            </if>
            <if test="province != null">
                province = #{province},
            </if>
            <if test="city != null">
                city = #{city},
            </if>
            <if test="district != null">
                district = #{district},
            </if>
            <if test="detailAddress != null and detailAddress != ''">
                detail_address = #{detailAddress},
            </if>
            <if test="tag != null">
                tag = #{tag},
            </if>
            <if test="isDefault != null">
                is_default = #{isDefault},
            </if>
            update_time = NOW()
        </set>
        WHERE address_id = #{addressId} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="softDeleteById">
        UPDATE addresses
        SET deleted_at = NOW(), is_default = 0, update_time = NOW()
        WHERE address_id = #{addressId} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="clearDefaultByUserId">
        UPDATE addresses
        SET is_default = 0, update_time = NOW()
        WHERE user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="setDefaultAddress">
        UPDATE addresses
        SET is_default = 1, update_time = NOW()
        WHERE address_id = #{addressId} AND user_id = #{userId} AND deleted_at IS NULL
    </update>
</mapper>

