<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.brewnow.mapper.OrderItemMapper">

    <!-- 订单项结果映射 -->
    <resultMap id="orderItemResultMap" type="com.brewnow.entity.OrderItem">
        <id property="orderItemId" column="order_item_id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="unitPrice" column="unit_price"/>
        <result property="subtotal" column="subtotal"/>
        <result property="productNameSnapshot" column="product_name_snapshot"/>
        <result property="brandSnapshot" column="brand_snapshot"/>
        <result property="categorySnapshot" column="category_snapshot"/>
        <result property="imageUrlSnapshot" column="image_url_snapshot"/>
        <result property="reviewed" column="reviewed"/>
    </resultMap>



    <!-- 基础SQL片段 -->
    <sql id="orderItemColumns">
        oi.order_item_id, oi.order_id, oi.product_id, oi.quantity, oi.unit_price, oi.subtotal,
        oi.product_name_snapshot, oi.brand_snapshot, oi.category_snapshot, oi.image_url_snapshot,
        CASE WHEN pr.review_id IS NULL THEN 0 ELSE 1 END AS reviewed
    </sql>

    <!-- 根据ID查询订单项 -->
    <select id="selectById" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        WHERE oi.order_item_id = #{orderItemId}
    </select>

    <!-- 根据订单ID查询订单项列表 -->
    <select id="selectByOrderId" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        WHERE oi.order_id = #{orderId}
        ORDER BY oi.order_item_id
    </select>

    <!-- 根据商品ID查询订单项列表 -->
    <select id="selectByProductId" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        WHERE oi.product_id = #{productId}
        ORDER BY oi.order_item_id DESC
    </select>

    <select id="selectLatestReviewableByUserAndProduct" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        JOIN orders o ON o.order_id = oi.order_id
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        WHERE o.user_id = #{userId}
          AND oi.product_id = #{productId}
          AND o.order_status IN ('SHIPPED', 'DELIVERED')
          AND pr.review_id IS NULL
          AND o.deleted_at IS NULL
        ORDER BY o.order_date DESC, oi.order_item_id DESC
        LIMIT 1
    </select>

    <!-- 查询所有订单项 -->
    <select id="selectAll" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        ORDER BY oi.order_item_id DESC
    </select>

    <!-- 分页查询订单项 -->
    <select id="selectByPage" resultMap="orderItemResultMap">
        SELECT <include refid="orderItemColumns"/>
        FROM order_items oi
        LEFT JOIN product_reviews pr ON pr.order_item_id = oi.order_item_id AND pr.deleted_at IS NULL
        ORDER BY oi.order_item_id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 插入订单项 -->
    <insert id="insert" parameterType="com.brewnow.entity.OrderItem" useGeneratedKeys="true" keyProperty="orderItemId">
        INSERT INTO order_items (
            order_id, product_id, quantity, unit_price, subtotal,
            product_name_snapshot, brand_snapshot, category_snapshot, image_url_snapshot
        ) VALUES (
            #{orderId}, #{productId}, #{quantity}, #{unitPrice}, #{subtotal},
            #{productNameSnapshot}, #{brandSnapshot}, #{categorySnapshot}, #{imageUrlSnapshot}
        )
    </insert>

    <!-- 根据ID更新订单项 -->
    <update id="updateById" parameterType="com.brewnow.entity.OrderItem">
        UPDATE order_items
        <set>
            <if test="quantity != null">
                quantity = #{quantity},
            </if>
            <if test="unitPrice != null">
                unit_price = #{unitPrice},
            </if>
            <if test="subtotal != null">
                subtotal = #{subtotal},
            </if>
            <if test="productNameSnapshot != null">
                product_name_snapshot = #{productNameSnapshot},
            </if>
            <if test="brandSnapshot != null">
                brand_snapshot = #{brandSnapshot},
            </if>
            <if test="categorySnapshot != null">
                category_snapshot = #{categorySnapshot},
            </if>
            <if test="imageUrlSnapshot != null">
                image_url_snapshot = #{imageUrlSnapshot},
            </if>
        </set>
        WHERE order_item_id = #{orderItemId}
    </update>

    <!-- 根据ID删除订单项 -->
    <delete id="deleteById">
        DELETE FROM order_items WHERE order_item_id = #{orderItemId}
    </delete>

    <!-- 根据订单ID删除订单项 -->
    <delete id="deleteByOrderId">
        DELETE FROM order_items WHERE order_id = #{orderId}
    </delete>

    <!-- 统计订单项总数 -->
    <select id="countAll" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM order_items
    </select>

    <!-- 根据订单ID统计订单项数量 -->
    <select id="countByOrderId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM order_items
        WHERE order_id = #{orderId}
    </select>

</mapper> 
